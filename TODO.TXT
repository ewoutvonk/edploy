- minimal_output integrated with hooks like deploy hooks, but then for output

--

- add bundle_path and bundle_without to deploy.yml + configuration questions
- make prepare tasks standard part of deploygen, turn it on/off in deploy.yml

--

- dploy-system gem, with system wide binscript for launching deployes with dploy command, which runs 'bundle exec dploy' which is contained in dploy gem, which in turn runs correct cap deploy..
- check whether dploy-system is needed, or if we can do with just 1 dploy gem, which is only installed system wide, like with bundler
